{
    "name": "docker-hub-image-tracker",
    "version": "0.1.0",
    "type": "module",
    "description": "Track new/updated Docker Hub image tags for a repository via the official Docker Hub API.",
    "engines": {
        "node": ">=18.0.0"
    },
    "dependencies": {
        "apify": "^3.2.6"
    },
    "scripts": {
        "start": "node src/main.js"
    }
}
